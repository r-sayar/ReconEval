# Minimal pip-only environment for running the metrics tutorial
# (tutorials/metrics.ipynb) end-to-end without GPU / FM dependencies.
#
# Install with:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r envs/requirements-min.txt
#   pip install -e .

numpy>=1.26
pandas>=2.2
scipy>=1.13
scikit-learn>=1.5
matplotlib>=3.8
seaborn>=0.13
tqdm>=4.66

# Single-cell I/O
anndata>=0.11
scanpy>=1.11
h5py>=3.10

# Metric resources (pathway / coexpression rely on these)
decoupler>=2.1
omnipath>=1.0
blitzgsea>=1.3

# Notebook execution
ipykernel
ipython
nbconvert
jupyter-client
