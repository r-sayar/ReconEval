meta:
  name: cellcycle


evaluator:
  _target_: sc_reconstruction.metrics._cellcycle.CellCycleBatchEvaluator
  _partial_: true
  mode: decode
  zarr_path: ${eval_path}
  emb_zarr_path: ${emb_zarr_path}
  min_cells: 20
  cell_cycle_genes_path: /lustre/groups/ml01/workspace/xiaotong.fu/data/reconstruction/sup/regev_lab_cell_cycle_genes.txt
  max_cells: 1e5
  split_key: ${split_key}
  batch_size: 500
  max_workers: 20
