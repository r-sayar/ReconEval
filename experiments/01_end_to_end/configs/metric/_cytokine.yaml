meta:
  name: cytokine


evaluator:
  _target_: sc_reconstruction.metrics._cytokine.CytokineBatchEvaluator
  _partial_: true
  mode: decode
  zarr_path: ${eval_path}
  emb_zarr_path: ${emb_zarr_path}
  cytokine_csv_path: /lustre/groups/ml01/workspace/xiaotong.fu/data/reconstruction/sup/cytokine_act_merged.csv
  min_genes: 5
  ctrl_size: 50
  n_bins: 25
  reducer: mean
  max_cells: 1e5
  split_key: ${split_key}
  batch_size: 500
  max_workers: 20
