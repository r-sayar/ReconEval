name: luca


path: "/lustre/groups/ml01/workspace/xiaotong.fu/data/reconstruction/lucav2/${split}"
eval_path: "/lustre/groups/ml01/workspace/xiaotong.fu/data/reconstruction/lucav2/comb_w_obs/ct_ds_orig.zarr"
split_comb: /lustre/groups/ml01/workspace/xiaotong.fu/data/reconstruction/lucav2/${split}/split_metadata.zarr

input_dim: 5983
split_key:
  - cell_type
  - dataset
  - origin

model_specific:
  PCA:
    _target_: sc_reconstruction.dataloaders.datamodules.DaskPCADataModule
    train_path: ${data.path}/train.h5ad
    chunk_size: 10_000
  scVI:
    _target_: sc_reconstruction.dataloaders.datamodules.IterDaskDataModule 
    train_path: ${data.path}/train.zarr
    val_path: ${data.path}/val.zarr
    test_path: ${data.path}/test.zarr
    chunk_size: 10_000
    num_workers: 16
    chunks_per_worker: 5
    minibatch_size: 256
  nlscVI:
    _target_: sc_reconstruction.dataloaders.datamodules.IterDaskDataModule 
    train_path: ${data.path}/train.zarr
    val_path: ${data.path}/val.zarr
    test_path: ${data.path}/test.zarr
    chunk_size: 10_000
    num_workers: 16
    chunks_per_worker: 5
    minibatch_size: 256
  mlscVI:
    _target_: sc_reconstruction.dataloaders.datamodules.IterDaskDataModule 
    train_path: ${data.path}/train.zarr
    val_path: ${data.path}/val.zarr
    test_path: ${data.path}/test.zarr
    chunk_size: 10_000
    num_workers: 16
    chunks_per_worker: 5
    minibatch_size: 256
  AE:
    _target_: sc_reconstruction.dataloaders.datamodules.IterDaskDataModule
    train_path: ${data.path}/train.zarr
    val_path: ${data.path}/val.zarr
    test_path: ${data.path}/test.zarr
    chunk_size: 10_000
    num_workers: 16
    chunks_per_worker: 5
    minibatch_size: 256
  olAE:
    _target_: sc_reconstruction.dataloaders.datamodules.IterDaskDataModule
    train_path: ${data.path}/train.zarr
    val_path: ${data.path}/val.zarr
    test_path: ${data.path}/test.zarr
    chunk_size: 10_000
    num_workers: 16
    chunks_per_worker: 5
    minibatch_size: 256
  mlAE:
    _target_: sc_reconstruction.dataloaders.datamodules.IterDaskDataModule
    train_path: ${data.path}/train.zarr
    val_path: ${data.path}/val.zarr
    test_path: ${data.path}/test.zarr
    chunk_size: 10_000
    num_workers: 16
    chunks_per_worker: 5
    minibatch_size: 256
  VQVAE:
    _target_: sc_reconstruction.dataloaders.datamodules.IterDaskDataModule
    train_path: ${data.path}/train.zarr
    val_path: ${data.path}/val.zarr
    test_path: ${data.path}/test.zarr
    chunk_size: 10_000
    num_workers: 16
    chunks_per_worker: 5
    minibatch_size: 256
  olVQVAE:
    _target_: sc_reconstruction.dataloaders.datamodules.IterDaskDataModule
    train_path: ${data.path}/train.zarr
    val_path: ${data.path}/val.zarr
    test_path: ${data.path}/test.zarr
    chunk_size: 10_000
    num_workers: 16
    chunks_per_worker: 5
    minibatch_size: 256
  mlVQVAE:
    _target_: sc_reconstruction.dataloaders.datamodules.IterDaskDataModule
    train_path: ${data.path}/train.zarr
    val_path: ${data.path}/val.zarr
    test_path: ${data.path}/test.zarr
    chunk_size: 10_000
    num_workers: 16
    chunks_per_worker: 5
    minibatch_size: 256
  DRVI:
    _target_: sc_reconstruction.dataloaders.datamodules.IterDaskDataModule
    train_path: ${data.path}/train.zarr
    val_path: ${data.path}/val.zarr
    test_path: ${data.path}/test.zarr
    chunk_size: 10_000
    num_workers: 16
    chunks_per_worker: 5
    minibatch_size: 256