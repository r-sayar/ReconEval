# configs/data/tahoe.yaml
name: tahoe

path: "/lustre/groups/ml01/workspace/xiaotong.fu/data/reconstruction/tahoe/${split}"
eval_path: "/lustre/groups/ml01/workspace/xiaotong.fu/data/reconstruction/tahoe/comb_w_obs_3000wCCM.zarr"
split_comb: /lustre/groups/ml01/workspace/xiaotong.fu/data/reconstruction/tahoe/${split}/split_metadata.zarr

input_dim: 6087
split_key:
  - cell_line
  - drug
  - dosage

model_specific:
  PCA:
    _target_: sc_reconstruction.dataloaders.datamodules.DaskPCADataModule
    train_path: ${data.path}/train.h5ad
    chunk_size: 1_000_000
  scVI:
    _target_: sc_reconstruction.dataloaders.datamodules.IterDaskDataModule 
    train_path: ${data.path}/train.zarr
    val_path: ${data.path}/val.zarr
    test_path: ${data.path}/test.zarr
    chunk_size: 50_000
    chunks_per_worker: 5
    minibatch_size: 5_000
  nlscVI:
    _target_: sc_reconstruction.dataloaders.datamodules.IterDaskDataModule 
    train_path: ${data.path}/train.zarr
    val_path: ${data.path}/val.zarr
    test_path: ${data.path}/test.zarr
    chunk_size: 50_000
    chunks_per_worker: 5
    minibatch_size: 5_000
  mlscVI:
    _target_: sc_reconstruction.dataloaders.datamodules.IterDaskDataModule 
    train_path: ${data.path}/train.zarr
    val_path: ${data.path}/val.zarr
    test_path: ${data.path}/test.zarr
    chunk_size: 50_000
    chunks_per_worker: 5
    minibatch_size: 5_000
  AE:
    _target_: sc_reconstruction.dataloaders.datamodules.IterDaskDataModule
    train_path: ${data.path}/train.zarr
    val_path: ${data.path}/val.zarr
    test_path: ${data.path}/test.zarr
    chunk_size: 50_000
    chunks_per_worker: 5
    minibatch_size: 5_000
  olAE:
    _target_: sc_reconstruction.dataloaders.datamodules.IterDaskDataModule
    train_path: ${data.path}/train.zarr
    val_path: ${data.path}/val.zarr
    test_path: ${data.path}/test.zarr
    chunk_size: 50_000
    chunks_per_worker: 5
    minibatch_size: 5_000
  mlAE:
    _target_: sc_reconstruction.dataloaders.datamodules.IterDaskDataModule
    train_path: ${data.path}/train.zarr
    val_path: ${data.path}/val.zarr
    test_path: ${data.path}/test.zarr
    chunk_size: 25_000
    chunks_per_worker: 5
    minibatch_size: 1_000
  VQVAE:
    _target_: sc_reconstruction.dataloaders.datamodules.IterDaskDataModule
    train_path: ${data.path}/train.zarr
    val_path: ${data.path}/val.zarr
    test_path: ${data.path}/test.zarr
    chunk_size: 30_000
    chunks_per_worker: 3
    minibatch_size: 1_000
  olVQVAE:
    _target_: sc_reconstruction.dataloaders.datamodules.IterDaskDataModule
    train_path: ${data.path}/train.zarr
    val_path: ${data.path}/val.zarr
    test_path: ${data.path}/test.zarr
    chunk_size: 30_000
    chunks_per_worker: 3
    minibatch_size: 1_000
  DRVI:
    _target_: sc_reconstruction.dataloaders.datamodules.IterDaskDataModule
    train_path: ${data.path}/train.zarr
    val_path: ${data.path}/val.zarr
    test_path: ${data.path}/test.zarr
    minibatch_size: 512