#!/usr/bin/env nextflow
/*
 * Placeholder entrypoint. Run compiled workflows from target/nextflow after:
 *   viash ns build --parallel --setup cachedbuild
 */

workflow {
  println "Use target/nextflow/workflows/*/main.nf — see openproblems/README.md"
}
